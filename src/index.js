import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';



// Rendering the App component inside the DOM element with the id 'app'
ReactDOM.createRoot(
    document.getElementById('app')
).render(<App />);
